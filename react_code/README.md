Personal website made from scratch using Reactjs.

Since github says, user pages must be built from the master branch, the build is present in master while the react code can be found in folder 'react_code'.